[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	name = Toby Scott
	email = hi@tobyscott.dev
[init]
	defaultBranch = main
[core]
	editor = vim
