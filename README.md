# Dotfiles - my GNU/Linux config files


## Current configurations
- [Arch Linux Desktop](/arch-desktop)
- *Others will be listed here*


## Installing a configuration

Each configured environment will have it's own `README.md` file which will include the instructions to install it (as well as a manual on how to use the configured environment with key-bindings, etc)

I hope to have an `./install.sh` script for each configuration to automate the installation process.